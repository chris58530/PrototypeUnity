using UnityEngine;

namespace _.Scripts.Temporary.State
{
    public enum PlayerState
    {
        Idle,
        Walk,
        Attack,
        Pull,
        Dash,
        Hurt,
        Roll,
        Dead
    }
    public class PlayerStateMachine : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
